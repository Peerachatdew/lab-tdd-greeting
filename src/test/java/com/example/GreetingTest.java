package com.example;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;

class GreetingTest {

    @Test
    void testHelloBasic() {
        // ตอนนี้เราจะ "แกล้ง" ให้มันพัง
        assertEquals("Hello, World", "test failed");
    }
}
```

        **อธิบาย:**
        - `assertEquals("Hello, World", "test failed");`
        - ตรวจว่า `"Hello, World"` (ซ้าย) **เท่ากับ** `"test failed"` (ขวา) หรือไม่
- **มันไม่เท่ากัน** → เทสต้องพัง! ❌

        ---

        ### **ขั้นตอนที่ 2.3: Run Test**

        1. **ดูที่โค้ด** จะเห็น**ลูกศรสีเขียว** ⏵ ข้าง ๆ บรรทัด `@Test` และ `class GreetingTest`

        2. **คลิกลูกศรสีเขียว** ⏵ ข้าง `@Test`

        3. เลือก **Run 'testHelloBasic()'**

        ---

        ### **ผลลัพธ์ที่ต้องการ:**

จะเห็น:
        - **แถบด้านล่างสีแดง** 🔴
        - ข้อความ: `expected: <Hello, World> but was: <test failed>`
        - **Test Failed** ✅ (นี่คือสิ่งที่เราต้องการ!)

        ---

        ## 📸 สรุปภาพรวม:
        ```
src/test/java
  └── com.example
      └── GreetingTest.java  ← สร้างไฟล์นี้